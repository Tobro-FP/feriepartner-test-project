export type Employee = {
  id: string; // json-server always converts id to string (v1.0+)
  name: string;
  position: string;
  department: string;
  email: string;
  phone: string;
  image: string;
};
